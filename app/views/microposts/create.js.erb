$("#microposts").html("<%= current_user.microposts.count %>")
var html = "<%= j render partial: 'shared/feed_item', locals: {feed_item: @feed_item}  %>"
$(html).prependTo(".microposts").effect("highlight", {}, 3000)
$(document).ready(function(){
	$("#new_post_form")[0].reset()
})
